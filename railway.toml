# railway.toml — Railway deployment config for AFP .NET 8 frontend
[build]
  builder = "NIXPACKS"

[deploy]
  startCommand = "dotnet AFP.dll"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3

[[services]]
  name = "afp-frontend"
