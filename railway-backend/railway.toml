# railway.toml  –  Railway deployment config for the AFP API
[build]
  builder = "NIXPACKS"

[deploy]
  startCommand = "node server.js"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3

[[services]]
  name = "afp-api"
